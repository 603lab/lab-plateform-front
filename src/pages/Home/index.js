import React, { PureComponent } from 'react';

export default class Home extends PureComponent {
  constructor(args) {
    super(args);

    this.state = {};
  }

  render() {
    return <div>Home</div>;
  }
}
